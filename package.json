{
  "name": "pfs-website",
  "version": "1.0.0",
  "private": true,
  "description": "Peninsula Fermentation Society — club website",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port 8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
